<Page
    x:Class="KanbanTasker.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="using:KanbanTasker.Helpers"
    xmlns:kbcontrol="using:KanbanTasker.Views"
    xmlns:local="using:KanbanTasker"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Windows.UI.Xaml.Controls"
    xmlns:syncfusion="using:Syncfusion.UI.Xaml.Kanban"
    xmlns:textconverters="using:KanbanTasker.Converters.Text"
    mc:Ignorable="d">
    <Page.Resources />
    <Grid Background="{ThemeResource AppBarBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid
            x:Name="AppTitleBar"
            Grid.Row="0"
            Background="{ThemeResource SystemControlChromeMediumLowAcrylicElementMediumBrush}">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />

            </Grid.ColumnDefinitions>
            <Image
                Grid.Column="0"
                Margin="8,8,8,2"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Source="ms-appx:///Resources/Images/KanbanTasker_Logo.png" />
        </Grid>
        <NavigationView
            x:Name="nvSample"
            Grid.Row="1"
            IsBackButtonVisible="Collapsed"
            IsSettingsVisible="False"
            PaneDisplayMode="Top">

            <NavigationView.PaneFooter>
                <AppBarButton
                    Width="40"
                    Icon="Delete"
                    ToolTipService.ToolTip="Delete Current Board" />
            </NavigationView.PaneFooter>
            <NavigationView.MenuItems>
                <NavigationViewItem Content="Board1" Tag="SamplePage1" />
                <NavigationViewItem Content="Board2" Tag="SamplePage2" />
                <NavigationViewItem Content="Board3" Tag="SamplePage3" />
                <NavigationViewItem Content="Board4" Tag="SamplePage4" />
                <NavigationViewItem Content="Board1" Tag="SamplePage1" />
                <NavigationViewItem Content="Board2" Tag="SamplePage2" />
                <NavigationViewItem Content="Board3" Tag="SamplePage3" />
            </NavigationView.MenuItems>
            <NavigationView.PaneCustomContent>
                <AppBarButton
                    Width="40"
                    Icon="Add"
                    ToolTipService.ToolTip="Create Board" />
            </NavigationView.PaneCustomContent>
            <Frame x:Name="contentFrame" />
        </NavigationView>
        <Grid Grid.Row="2" Background="{ThemeResource RegionBrush}">
            <Rectangle
                Height="8"
                HorizontalAlignment="Stretch"
                IsHitTestVisible="False"
                Opacity="0.2"
                Visibility="Visible">
                <Rectangle.Fill>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Offset="1" />
                        <GradientStop Offset="0" Color="Black" />
                    </LinearGradientBrush>
                </Rectangle.Fill>
            </Rectangle>
        </Grid>
        <kbcontrol:BoardView Grid.Row="3" />
    </Grid>
</Page>
